'use client';
import {useState} from 'react';
import {ArrowUpRight,Mail,MapPin} from 'lucide-react';
export default function Contact(){const [ready,setReady]=useState(false);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const body=`Name: ${d.get('name')}\nEmail: ${d.get('email')}\nCity: ${d.get('city')}\n\n${d.get('message')}`;window.location.href=`mailto:info@mrpaints.in?subject=${encodeURIComponent('Website enquiry from '+d.get('name'))}&body=${encodeURIComponent(body)}`;setReady(true)}return <main id="main"><section className="page-intro"><span className="eyebrow">CONTACT MR PAINTS</span><h1>Let's talk<br/><em>possibilities.</em></h1><p>A product question, a new project or a partnership in mind? We'd love to hear from you.</p></section><section className="contact-section"><aside><h2>A conversation<br/>starts here.</h2><div className="contact-detail"><Mail/><div><span>Email us</span><a href="mailto:info@mrpaints.in">info@mrpaints.in ?</a></div></div><div className="contact-detail"><MapPin/><div><span>Based in</span><b>India</b></div></div><p>For product and trade enquiries, include your city and requirements so we can understand what you need.</p></aside><form onSubmit={submit}><h2>Tell us what you have in mind.</h2><div className="form-row"><label>Your name<input name="name" autoComplete="name" placeholder="Full name" required/></label><label>Email address<input name="email" type="email" autoComplete="email" placeholder="you@example.com" required/></label></div><label>City<input name="city" autoComplete="address-level2" placeholder="Your city, India" required/></label><label>Your enquiry<textarea name="message" rows={5} placeholder="Tell us about your project, product needs or business…" required/></label><p className="form-note">This opens your email app with your enquiry ready to send.</p><button className="button" type="submit">Prepare email <ArrowUpRight size={19}/></button>{ready&&<p className="form-status" role="status">Your email draft is ready in your email app. Please send it there. If an app didn't open, email info@mrpaints.in directly.</p>}</form></section></main>}
